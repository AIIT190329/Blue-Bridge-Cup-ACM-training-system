//题目描述
//编写一个程序，输入a、b、c三个值，输出其中最大值。
//输入
//一行数组，分别为a b c
//输出
//a b c其中最大的数
//样例输入
//10 20 30
//样例输出
//30


#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main()
{
	int a = 0, b = 0, c = 0, max = 0;
	scanf("%d%d%d", &a, &b, &c);
	max = a > b ? (a > c ? a : c) : (b > c ? b : c);
	printf("%d\n", max);
	return 0;
}
